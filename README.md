Funktionen Gui.py
    - ausgabe(inp:str)
    - print_spielfeld(spielfeld:dict[list[str]])
    - spielzug() -> int
    - spieler_abfragen() -> list[str]

Funktion Main.py
    - gewinner_ermitteln(spielfeld:dict[list[str]]) -> bool
    - unentschieden_ermitteln(spielfeld:dict[list[str]]) -> bool
